<Window
    x:Class="UpworkPdfGenerator.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Pdf Generator"
    Width="800"
    Height="250"
    >
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label
            Grid.Row="0"
            Grid.Column="0"
            Content="Sign:"
            />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            x:Name="SignTextBox"
            />
        <Label
            Grid.Row="1"
            Grid.Column="0"
            Content="Contractor(rus):"
            />
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            x:Name="ContractorRusTextBox"
            />
        <Label
            Grid.Row="2"
            Grid.Column="0"
            Content="Contractor(eng):"
            />
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            x:Name="ContractorEngTextBox"
            />
        <Label
            Grid.Row="3"
            Grid.Column="0"
            Content="Value:"
            />
        <TextBox
            Grid.Row="3"
            Grid.Column="1"
            x:Name="ValueTextBox"
            />
        <Button
            Grid.Row="4"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Content="Create"
            Click="Button_Click"
            />
    </Grid>
</Window>
